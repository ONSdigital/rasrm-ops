replicaCount: 1

env: "minikube"

service:
  port: 8003

uaa:
  client_id: "test-ops-id"
  client_secret: "super-secret"

resources:
  requests:
    cpu: "500m"
    memory: "128Mi"
  limits:
    cpu: "1000m"
    memory: "256Mi"
